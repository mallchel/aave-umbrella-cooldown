name: OpenAPI Sync Check

on:
  pull_request:
  push:

jobs:
  openapi-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify OpenAPI JSON is in sync
        run: make openapi-check
